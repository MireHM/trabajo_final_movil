name: inventory_system
description: Sistema de inventario offline-first para tienda de electrónicos
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.4.9
  
  # Database
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.1
  path: ^1.8.3
  
  # Backend
  supabase_flutter: ^2.0.0
  
  # Environment
  flutter_dotenv: ^5.1.0
  
  # UI
  google_fonts: ^6.1.0
  intl: ^0.18.1
  fl_chart: ^0.65.0
  
  # Utils
  uuid: ^4.2.2

  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  drift_dev: ^2.14.0
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  assets:
    - .env
